const form = document.querySelector("main");

function spawnTabs(e){
    const headerTab = document.getElementById("headertab").value;
    headertab.innerHTML += `<button class="tablinks">th:field="*{CompanyName}"</button>`;
  };
  form.appendChild();

function RetrieveFragments(var url, var targetDiv) = {
    if ($('#searchSurname').val() != ''){
        url = url + '/' + $('#searchSurname').val();
       }

    $(targetDiv).load(url);
}

//Add an event listener with which I can listen for click of the save button





